<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fastcampus.toyboard.repository.UserRepository">
    <select id="findAll" resultType="com.fastcampus.toyboard.dto.UserRoleDto">
        select ut.id,
               ut.username,
               ut.email,
               ut.nickname,
               ut.created_at,
               (select role from role_tb where ut.id = role_tb.user_id) as role
        from user_tb as ut
    </select>

    <update id="updateById">
        update role_tb
        set role = {#role}
        where user_tb.id = role_tb.user_id
    </update>
</mapper>